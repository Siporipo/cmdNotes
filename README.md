# cmdNotes
## Installation Guide
To install cmdNotes simply place the notes executable found in bin/ along with noteconf.txt in a stable place, maybe add the directory to PATH, so you can call it with plain 'notes' instead of './pathtonotes/notes'
